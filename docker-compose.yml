services:
  test-sellaro-landing:
    build: .
    ports:
      - "8079:5011"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: testing-app-network